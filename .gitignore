.env
.env.*
*.env
.envrc
!.env.example

secrets/
*.key
*.pem

node_modules/
data/*.db
data/*.db-*
data/match_snapshot_runs.json

.DS_Store
*.log
*.sync-conflict-*

# Runtime artifacts
*.old
db/*.db
snapshots/*/data/*.db

# Local agent/editor tooling
.claude/settings.local.json
.claude/

# Runtime caches (regenerated at startup)
data/roster_cache.json

# JS build artifact (source is in static/js/*.js, run `npm run build:js` to regenerate)
static/js/bundle.js

# Local runtime outputs
logs/

# Broaden DB ignores
*.db
*.sqlite
*.sqlite3

# Historical source CSVs (support H2H import, not for public repo)
data/sources/world-cup-history/

# Temporary / exploratory files
scratch/

# Data exploration outputs
data/lineups_sync_runs.json

# AI tooling local config
.codebuddy/
.workbuddy/

# Temp / backup files
*.bak
scratch/previews/
docs/archive/
docs/deployment-strategy.md
