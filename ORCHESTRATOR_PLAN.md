# 🎛️ PitchSignal — 总调度方案

> **总调度**：AgentsOrchestrator  
> **日期**：2026-06-15  
> **审阅文件**：REQUIREMENTS_AUDIT.md + SPRINT_PLAN.md + ARCHITECTURE_REFACTOR_PLAN.md + SENIOR_PM_REVIEW.md  
> **项目地址**：http://192.168.2.231:5099  

---

## 一、项目全景摘要

**一句话总结：骨架精良但数据全假——35 个 API 端点已实现，但 6 个核心模块（球员、球队、盘口、新闻、历史交锋、预测输入）返回的全是 Math.random() 生成的 mock 数据，用户看到的每一条统计信息都毫无事实依据。当前评分 5.3/10，修完数据链路可达 7.0+。**

### 关键数字

| 指标 | 现状 | 目标 |
|------|------|------|
| 综合评分 | 5.3/10 | ≥7.0/10 |
| Mock 数据模块 | 6 个 | 0 个（真实或明确标注） |
| API 端点 | 35 个已实现 | 35 个全部返回真实数据 |
| server.js 行数 | 3111 行 | Sprint 1 不动，Sprint 2 降至 <500 |
| Step 1 完成度 | ✅ middleware 提取完成 | — |
| Step 2 完成度 | 🔜 数据层统一 | 当前 Sprint 首要任务 |

---

## 二、需求裁决（砍、保、改）

### ✅ 必须做（Sprint 1 强制项）

| # | 需求 | 理由 |
|---|------|------|
| 1 | ESPN roster 真实数据链路 | 所有球员/球队数据的根基，断了全盘皆假 |
| 2 | 盘口数据源替换或优雅降级 | 用户看到"隐含概率 45%"是随机数 = 产品信任归零 |
| 3 | 球员近 10 场真实数据 | `/api/player/:id/enhanced` 当前 100% mock |
| 4 | 球队增强数据真实化 | `/api/team/:id/enhanced` 当前 80% mock |
| 5 | 前端 SVG 对位阵型图 | 后端已完整实现，前端只差 SVG 渲染，最大差异化功能 |
| 6 | dataQuality 标记系统 | 比任何新功能都重要——诚实 > 好看 |
| 7 | 架构 Step 2：数据层统一 | 消除硬编码 coach/venue，提升数据质量 |

### ⬇️ 降级做（有时间才做）

| # | 需求 | 降级方案 |
|---|------|---------|
| 1 | 新闻聚合 | 接通 Tavily/RSS 任一真实源；若无 key 则明确标注"暂无新闻" |
| 2 | 角球追踪条 | 后端已就绪，前端组件 2-3h，排在 SVG 之后 |
| 3 | 预测 Tab 前端 | 展示 Elo+Poisson 输出 + 置信度标注 + 免责声明 |
| 4 | 历史交锋 | 有 ESPN 就用真数据，没有就标注"数据暂缺"替代 mock |
| 5 | 出线形势数学分析 | Monte Carlo 引擎已有，加置信区间和水印 |

### ✂️ 砍掉（Sprint 1 不做）

| # | 需求 | 理由 |
|---|------|------|
| 1 | AI 异动解读 | 需 LLM API + 盘口真数据，两个前提都不具备 |
| 2 | 盘口变化原因推测 | 盘口真数据都不通，推测是空谈 |
| 3 | ML 模型优化 | Garbage In Garbage Out，先修数据 |
| 4 | 淘汰赛升级盘口 API | 小组赛数据都没跑通 |
| 5 | 架构 Step 3-5（路由拆分/版本化/前端 API 层） | 推迟到 Sprint 2，当前时间应全投数据 |
| 6 | 预测历史/准确率追踪 | 需数据库支撑，先做内存缓存版 |
| 7 | 比赛环境综合分析 `/api/match/:id/context` | 场馆+天气端点已独立存在，聚合是 nice-to-have |
| 8 | `coach-legacy` 路由 | 210 行硬编码旧数据，Step 2 直接删除 |

---

## 三、任务调度表（按优先级排序）

### Phase A：架构收尾（Day 1）— 当前正在执行

| 任务 ID | 任务 | 负责角色 | 前置依赖 | 预估工时 | 验收标准 |
|---------|------|---------|---------|---------|---------|
| A1 | **完成架构 Step 2：数据层统一** | Backend Dev (🦞) | Step 1 ✅ | 1.5h | ① 创建 `data/loader.js` 统一加载 ratings/coaches/venues/team_names_zh ② 删除 `GET /api/coach-legacy/:teamId`（210 行硬编码）③ 删除 server.js L2006-2057 硬编码 venue 数据 ④ server.js 减少 ~270 行 ⑤ 所有端点 curl 返回不变 |
| A2 | **验证 Step 2 回归** | QA | A1 | 0.5h | 35 个端点冒烟测试全部 HTTP 200 |

### Phase B：数据链路打通（Day 2-5）— Sprint 1 核心

| 任务 ID | 任务 | 负责角色 | 前置依赖 | 预估工时 | 验收标准 |
|---------|------|---------|---------|---------|---------|
| B1 | **ESPN roster 数据源修复** | Backend Dev (🦞) | A1 | 4-6h | ① `/api/team/:id/roster` 返回真实球员名单（非空数组）② 字段完整率 >90%（id, name, position, number）③ 至少覆盖 10 支球队验证 |
| B2 | **球员增强数据真实化** | Backend Dev (🦞) | B1 | 4-6h | ① `/api/player/:id/enhanced` 的 recentForm/clubStats/nationalStats 来自 ESPN match log ② 不再出现 Math.random() ③ 无数据时返回 `dataQuality: "unavailable"` 而非假数据 |
| B3 | **球队增强数据真实化** | Backend Dev (🦞) | B1 | 3-4h | ① `/api/team/:id/enhanced` 的进攻/防守/控球指标来自真实统计 ② 或明确标注为 ratings 估算 + `dataQuality: "estimated"` |
| B4 | **盘口数据源替换** | Backend Dev (🦞) | 无 | 4-6h | ① 调研 3 个候选源（The Odds API 付费 / API-Football / Football-data.org）② 至少一个返回真实赔率 ③ 若全部不可用，实施优雅降级：`dataQuality: "unavailable"` + 隐藏假数据 |
| B5 | **历史交锋真实化** | Backend Dev (🦞) | B1 | 2-3h | ① 有 ESPN 数据则用真实交锋记录 ② 无数据则返回 `dataQuality: "unavailable"` 替代 mock 随机生成 |

### Phase C：前端差异化功能（Day 6-8）

| 任务 ID | 任务 | 负责角色 | 前置依赖 | 预估工时 | 验收标准 |
|---------|------|---------|---------|---------|---------|
| C1 | **SVG 对位阵型图渲染** | Frontend Dev (🦐) | B1（真实阵容） | 3-4h | ① 根据 `/api/matchup/:id/formation` 数据渲染 22 人球员点位 ② 支持至少 3 种常见阵型（4-3-3 / 4-4-2 / 3-5-2）③ 球员点位可点击查看详情 |
| C2 | **角球追踪条组件** | Frontend Dev (🦐) | 无 | 2-3h | ① 根据 `/api/corner-analysis/:id` 渲染进度条 ② 显示 actual vs expected 角球数 ③ 显示实时 pace 指标 |
| C3 | **预测 Tab 前端实现** | Frontend Dev (🦐) | B1, B2, B4 | 3-4h | ① 展示胜/平/负概率分布（Chart.js 饼图或条形图）② 展示关键因子（Elo 差、主场优势等）③ 底部免责声明 |

### Phase D：可信度与收尾（Day 9-10）

| 任务 ID | 任务 | 负责角色 | 前置依赖 | 预估工时 | 验收标准 |
|---------|------|---------|---------|---------|---------|
| D1 | **全端点添加 dataQuality 标记** | Backend Dev (🦞) | B1-B5 | 3-4h | ① 所有 API 响应包含 `_meta.dataQuality` 字段（`live`/`estimated`/`unavailable`）② 包含 `source` 和 `confidence` 字段 |
| D2 | **前端可信度标记 UI** | Frontend Dev (🦐) | D1 | 2-3h | ① 所有数据模块显示 🟢🟡🔴 标记 ② 用户 hover 可见数据来源说明 |
| D3 | **全量回归测试** | QA + 全栈 | D1, D2 | 2-3h | ① 35 个 API 全部通过冒烟测试 ② 核心页面无视觉回归 ③ 所有 mock 数据已标记或替换 |
| D4 | **新闻模块处理** | Backend Dev (🦞) | 无 | 2-3h | ① 若有 Tavily key：接通真实新闻源 ② 若无：返回"暂无相关新闻"而非 mock 假新闻 |

### Buffer（预留）

| 任务 ID | 任务 | 负责角色 | 说明 |
|---------|------|---------|------|
| BUF | 未完成项兜底 + 性能优化 | 全栈 | 处理 Sprint 超时任务；若无超时，优化首屏加载和 Chart.js 渲染性能 |

---

## 四、依赖关系图

```
                    ┌─────────────┐
                    │  A1: 数据层  │
                    │   统一       │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  B1: ESPN   │ ← B4 盘口（独立并行）
                    │  roster 修复 │
                    └──┬───┬───┬──┘
                       │   │   │
              ┌────────┘   │   └────────┐
              ▼            ▼            ▼
        ┌──────────┐ ┌──────────┐ ┌──────────┐
        │ B2:球员  │ │ B3:球队  │ │ B5:交锋  │
        │ 数据真实 │ │ 数据真实 │ │ 数据真实 │
        └────┬─────┘ └────┬─────┘ └──────────┘
             │            │
             └─────┬──────┘
                   ▼
        ┌─────────────────────┐
        │ C1: SVG 对位阵型图  │ ← C2 角球追踪条（独立并行）
        └─────────┬───────────┘
                  │
        ┌─────────▼───────────┐
        │ C3: 预测 Tab 前端   │
        └─────────┬───────────┘
                  │
        ┌─────────▼───────────┐
        │ D1: dataQuality 标记│
        └─────────┬───────────┘
                  │
        ┌─────────▼───────────┐
        │ D2: 前端可信度 UI   │
        └─────────┬───────────┘
                  │
        ┌─────────▼───────────┐
        │ D3: 全量回归测试    │
        └─────────────────────┘
```

---

## 五、风险预警

| 风险 | 等级 | 概率 | 影响 | 缓解措施 |
|------|------|------|------|---------|
| **ESPN API 无公开免费接口** | 🔴 | 高 | B1-B3 全部阻塞 | Plan B：手动维护静态 roster JSON + 定期更新脚本；调研 ESPN Core API 第三方封装（SportMonks 等）|
| **盘口数据源全部不可用** | 🔴 | 高 | B4 阻塞 | Day 4 硬截止：3 个候选源均不可用时立即实施优雅降级，不消耗更多时间 |
| **ESPN roster 返回空数组** | 🟡 | 中 | B1 无法验证 | 排查端点路径是否变更；用 `/athletes` 端点重建 roster |
| **无 xG/xA 真实数据源** | 🟡 | 中 | 球员/球队增强数据精度低 | 用 ESPN match log 替代，精度有限但优于 mock |
| **Sprint 1 时间不够** | 🟡 | 中 | 部分任务延期 | 严格按 P0→P1→P2 推进；D4（新闻）可砍；Buffer Day 兜底 |
| **架构 Step 2 引入回归** | 🟢 | 低 | A1 代码出错 | 仅替换数据加载方式，不改路由逻辑；回归测试覆盖 |
| **世界杯赛程进行中，时间窗口紧** | 🔴 | 现实 | 用户每天看到假数据 | 这是最大的外部压力——每多一天假数据，产品信誉多损一天 |

---

## 六、给项目负责人的一封信

> 项目负责人，你好：
>
> 读完四份报告，结论很清楚：**这不是一个功能问题，而是一个信任问题。** 35 个 API 端点、92% 功能覆盖——骨架很好。但 6 个核心模块返回的是 Math.random() 生成的假数据。用户看到"德国近 10 场场均 1.8 球"时，不知道这个数字是抛骰子来的。
>
> **建议很明确：暂停架构深水区，全力打通数据任督二脉。** Step 1+2 已经在跑，做完就停。接下来 5 天只做一件事——让 ESPN roster 返回真实球员，让盘口要么返回真赔率、要么诚实说"暂无"。数据通了再做前端差异化功能。模块化拆分推到 Sprint 2。
>
> **记住这个公式：数据真实 + 架构乱 = 产品有价值；数据假 + 架构美 = 零。**
>
> — AgentsOrchestrator

---

## 七、执行指令（给下一个执行 Agent）

**当前状态**：架构 Step 1 已完成（middleware/ 3 个文件 + services/espn.js 已创建，server.js 已 require）。

**立即执行**：

1. **先做 A1**（架构 Step 2：数据层统一）— 创建 `data/loader.js`，消除硬编码 coach-legacy 和 venue 数据
2. **验证 A2**（冒烟测试 35 端点）
3. **然后做 B1**（ESPN roster 修复）— 这是整个 Sprint 的瓶颈，越早开始越好
4. **B4（盘口）与 B1 可并行调研**，但实现等 B1 完成后

**不要做的事**：
- ❌ 不要做架构 Step 3-5（路由拆分/版本化/前端 API 层）
- ❌ 不要重构 index.html 结构
- ❌ 不要在 mock 数据上做前端美化
- ❌ 不要花时间在 AI 异动解读、ML 模型优化上

**时间硬约束**：Sprint 1 = 10 个工作日。Day 10 结束时必须达到 ≥7.0/10。

---

*总调度：AgentsOrchestrator | 2026-06-15 03:06 CST*
